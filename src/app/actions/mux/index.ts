import { createSignToken } from "./createSignToken";
import { getPlaceholder } from "./getPlaceholder";
import { createUploadUrl } from "./upload";

export const muxActions = {
  createUploadUrl,
  createSignToken,
  getPlaceholder,
};
